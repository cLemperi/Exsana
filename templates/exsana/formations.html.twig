{% extends 'base.html.twig' %}

{% block title %}Hello ExanaHomeController!{% endblock %}

{% block body %}

<div class="container-fluid background-formation">
	<div class="formation-core container">
		<div class="py-5">
			<div class="title_formation"><h1>Formations Exsana santé<h1></div>
		</div>
		<!--
			<br><br>
				<div class="col-md-3 order-md-2 mb-4">
					<h4 class="d-flex justify-content-between align-items-center mb-3">
						<span class="title-category">Recherche</span>
					</h4>
				{# {% include 'formation/_filter.html.twig' with {form: form} only %}	#}
				</div>
			<div class="count">
			</div>
		-->
				<div class="listFormations">
					<section class="listFormations">
						{% for formation in formations %}
						<div class=" d-flex flex-column formation-item">
							<div class="p2 d-flex">
								<a class="full-link" href="{{ path('formation', {id: formation.id}) }}">
								<div class="custom_title">
								<h2>{{ formation.title }}</h2></div></a>
							</div>
							<div class="d-flex align-items-center formation-cara">
								<div class=" formation-duration">DURÉE : {{ formation.durationFormation }} jours</div>
								<div class=" formation-enseignement">Présentielle</div>
								<div class=" note-etoile"></div>
							</div>
							<div class="d-flex d-flex align-items-end">
								<div class="formation-reference"> REF: A0A25</div>
							</div>

							
						</div>
						{% endfor %}
					</section>
					<div class="pagination">{{ knp_pagination_render(formations) }}</div>
				</div>
	</div>
</div>
{% endblock	%}